-- ============================================================
-- users.skill_tags を追加（COM-001 / COM-002 の「保有スキル」用）
--
-- 背景:
--   - 対応できる職種（user_skills.trade_type）は「型枠大工」「塗装」等の
--     大分類（TRADE_TYPES で列挙された固定値）
--   - 保有スキル（新設）は「型枠設置」「電気工」「送配電線工」等の
--     より詳細・自由入力のタグ。受注者が任意追加する
--   - これまで COM-001/002 では保有スキルの表示・編集欄が無く、
--     CLI-006 や applications/* では trade_type を「保有スキル」ラベルで
--     重複表示していた（暫定対応）
--
-- 設計判断:
--   - user_qualifications 同様に別テーブルにすることも検討したが、
--     タグ以上の属性（ID・作成日時）が不要のため text[] で十分と判断
-- ============================================================

ALTER TABLE users
  ADD COLUMN skill_tags text[] NOT NULL DEFAULT '{}';
