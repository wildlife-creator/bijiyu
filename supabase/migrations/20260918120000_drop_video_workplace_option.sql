-- ============================================================
-- 2026-09-18: 旧「職場紹介動画」オプション（option_type = 'video_workplace'）の廃止
--
-- P10（2026-09-10）で「プロフィール動画制作プラン」（video）に統合し新規販売を停止していたが、
-- リリース前で実購入者はテストユーザーのみのため、アプリのコードからキーごと削除する。
-- 既存行は統合先の 'video' に書き換え、DB 側にも旧キーを残さない
-- （Webhook・管理画面の絞り込み・料金画面の「購入済み」判定はすべて 'video' だけを見るようになる）。
-- ============================================================

UPDATE public.option_subscriptions
   SET option_type = 'video'
 WHERE option_type = 'video_workplace';
