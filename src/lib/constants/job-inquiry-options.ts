// ---------------------------------------------------------------------------
// Job inquiry topics (求人へのお問い合わせ / COM-013)
// ---------------------------------------------------------------------------
// 値はラベル文字列で保存する（マスタテーブル化しない＝将来の選択肢増減はコード
// 定数の変更で対応する。trouble-report / contact と同方針）。
// DB 保存値・Zod の z.enum() 入力・メールテンプレ表示で全て同じ定数を参照する。

export const INQUIRY_TOPICS = [
  "求人について話を聞きたい",
  "求人に応募したい",
  "その他",
] as const;

export type InquiryTopic = (typeof INQUIRY_TOPICS)[number];
