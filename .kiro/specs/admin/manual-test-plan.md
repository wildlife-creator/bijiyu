# 管理者機能（ADM-001〜024）手動テスト手順書

この手順書は、admin 機能を**1画面ずつ・複数セッションに分けて**手動で確認するためのガイドです。
自動テスト（Vitest / pgTAP / Playwright）は正常系とロジックを押さえています。ここでは
**見た目・操作感・実際のデータ表示**を、あなたの目で確かめることが目的です。

> 🖥️ **動作確認は PC（デスクトップ幅）で行ってください。** 管理画面は運営担当者の PC 利用を前提とした画面設計のため、スマホ幅でのレイアウト確認は対象外です（ユーザー向け画面とは方針が異なります）。

---

## このガイドの使い方

- 各ステップ先頭の `- [ ]` を、確認できたら `- [x]` に変えていってください（このファイルを直接編集してOK）。
- 「気になった点」は各セッションの末尾のメモ欄に書き留めてください。次セッションで私が修正します。
- **セッションは長くなりすぎる前に切り替えて大丈夫です。** 1セッション = 1ブロック（A〜G）を目安にしています。
- 新しいセッションを始めるときは、私（Claude）に「manual-test-plan.md の続き（ブロックX）をやりたい」と伝えてください。私がこのファイルの進捗を見て、環境を整えてからご案内します。
- 各詳細画面では、画面下部などの「もどる」で前の画面に戻れることも随時チェックしてください（admin 全画面で戻る導線が機能しているか）。

---

## セッション開始時の準備（毎回・私が代行します）

新セッションで手動テストを始めるとき、私が次を実行して環境をクリーンにします。あなたは確認するだけでOKです。

1. Supabase が起動しているか（`supabase status`）
2. 開発サーバーが起動しているか（`npm run dev` → http://127.0.0.1:3000）
3. **`supabase db reset`** でデータを初期状態に戻す（前回の操作跡をリセット）

> 💡 **なぜ毎回リセットするの？**
> 「承認」「削除」「発注取消」などは一度実行するとデータが変わります。
> セッションの最初に必ずリセットすれば、毎回まっさらな状態から確認できます。

---

## ログイン情報

| 用途 | メールアドレス | パスワード | 入口 |
|---|---|---|---|
| 管理者 | `admin@test.local` | `testpass123` | http://127.0.0.1:3000/admin/login |

---

## 使い捨てアカウント早見表（壊してOKなアカウント）

「戻せない操作」はこのアカウントで試してください。リセットすれば元に戻ります。

| 操作 | 対象アカウント / データ | 表示名 |
|---|---|---|
| 本人確認の**承認** | `adm-pending-identity@test.local` | 山本健（本人確認 pending） |
| 本人確認の**否認** | `adm-reject-identity@test.local` | 森大地（本人確認 pending #2） |
| CCUS の承認/否認 | `adm-pending-ccus@test.local` | 井上翔（CCUS pending） |
| 発注者の**単純削除** | `adm-del-individual@test.local` | 削除テスト個人（個人発注者・案件なし） |
| 発注者の**カスケード削除** | `adm-del-corp-owner@test.local` | 削除テスト法人（法人 Owner＋担当者1名） |
| 受注者アカウントの**削除** | `adm-del-contractor@test.local` | 削除受注（受注者・応募なし） |
| 応募の**発注取消** | 応募ID `ada00000-0000-4000-8000-000000000004` | 井上翔の応募（山田建設の発注済み案件・初回稼働日前） |
| 双方向の評価が入った応募 | 応募ID `aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa` | 田中一郎 × 鈴木工務店（ユーザー評価＋発注者評価あり・ADM-014 表示確認用） |
| 招待フロー | 任意の新規メール（例: `try-invite@test.local`） | 実際にメールが Mailpit に届きます |

> 💡 発注取消の対象応募は、セッション開始時の `supabase db reset` で「発注済み（accepted）」状態に戻ります。リセット前は前回の確認で取消済みになっていることがあります。

> ⚠️ **本番フィクスチャ（鈴木工務店・山田建設・中村リフォーム）は削除しないでください。**
> 他の確認項目が一緒に消えてしまいます。削除を試すときは必ず上の「削除テスト」アカウントで。

---

## ブロック A：ログイン・トップ・パスワード変更（ADM-001 / 002 / 015）

- [x] **A-1 ログインできる** — `/admin/login` で `admin@test.local` / `testpass123` を入力 → ログインボタン
  - 確認: 管理者トップページ（`/admin/dashboard`）に移動する
- [x] **A-2 間違ったパスワードは拒否される** — （A-1 でログイン済みなら一度ログアウトして）わざと違うパスワードでログイン
  - OKの目安: 「メールアドレスまたはパスワードが正しくありません」と出て、ログインできない
- [x] **A-3 一般ユーザーは入れない** — 一度ログアウトし、`contractor@test.local` / `testpass123` でログイン → アドレスバーに `/admin/dashboard` を直接入力
  - OKの目安: マイページに弾き返される（管理画面に入れない）
- [x] **A-3B 未ログインでは入れない** — ログアウトした状態で、アドレスバーに `/admin/dashboard` を直接入力
  - OKの目安: `/admin/login` に弾き返される
- [x] **A-4 トップの8メニュー＋下部リンクが並ぶ** — admin で再ログイン
  - 確認（8メニュー）: 発注者一覧／ユーザー一覧／本人確認／応募履歴／お問い合わせ／トラブル報告／求人問い合わせ／代理メッセージ一覧
  - 確認（下部リンク）: パスワード変更（小さめ）・ログアウト（小さめ・赤字）
  - 確認（ヘッダー）: 左上にロゴ画像／右上にハンバーガーメニュー（☰）
- [x] **A-4B ハンバーガーメニュー** — ヘッダー右上の☰を開く
  - 確認: トップと同じ8メニュー＋パスワード変更（小さめ）・ログアウト（小さめ・赤字）が並ぶ
- [x] **A-5 各メニューに移動できる** — 8つのメニュー（トップ or ハンバーガー）を順にクリック → それぞれの一覧画面が開く → ヘッダー左上のロゴでトップに戻れる
- [x] **A-6 ログアウトできる** — ヘッダー右上の☰ →「ログアウト」 → ログイン画面に戻る
- [x] **A-7 パスワード変更画面が動く** — `/admin/password`
  - 確認: 現在のパスワード／新しいパスワード／確認 の3項目がある。空欄や不一致でエラーが出る（※実際に変更すると admin のパスワードが変わるので、変更するならリセット前提で）
  - 現在のパスワード誤りの拒否（非破壊なので安全に試せる）: 現在のパスワードに**誤った値**＋新パスワード（確認と一致）を入れて送信 → 「現在のパスワードが正しくありません」と出て、パスワードは変わらない

**ブロック A メモ欄:**
- 全ステップOK（2026-06-16）。
- 修正対応済み: ①ヘッダー左上をテキスト→ロゴ画像に（クリックで管理者トップ）②ヘッダー右上にハンバーガーメニュー追加（トップと同じ8メニュー＋パスワード変更・ログアウト）③パスワード変更・ログアウトを小さめ文字に・ログアウトは赤字に（ハンバーガー／トップ両方）④「メッセージ一覧」→「代理メッセージ一覧」にリネーム（メニュー・見出し・E2E・仕様書）。※ロゴ横の「管理画面」併記はユーザー判断で今は付けない。
- 全管理画面の横幅: ウィンドウを広げると一覧系が画面端まで間延びする問題を、AdminLayout の `<main>` に `mx-auto w-full max-w-2xl` を入れて全画面一括で上限・中央寄せに対応（既存の詳細画面 max-w-2xl に統一。今後追加する画面も自動で適用される）。

---

## ブロック B：発注者アカウント管理（ADM-003 / 004 / 005）

- [x] **B-1 発注者一覧が見える** — トップ →「発注者アカウント一覧」（`/admin/clients`）
  - 確認: 氏名・会社名・メール・区分バッジ・プラン・オプションバッジ・「※退会済み」表示
  - 確認: キーワード欄の左に🔍アイコンがある
- [x] **B-2 キーワード検索** — 「鈴木」で検索 → 鈴木工務店の行に絞られる
- [x] **B-3 区分フィルタ** — 区分で「小規模発注者」を選んで検索 → 小規模プランの発注者（山田建設・木村工務店）に絞られる（法人の鈴木花子は消える）
- [x] **B-4 オプションフィルタ** — オプションで「急募」を選んで検索 → 山田建設が出る
- [x] **B-5 詳細に移動** — 鈴木工務店株式会社の行（鈴木花子）をクリック → 発注者アカウント詳細（`/admin/clients/...`）
- [x] **B-6 詳細のセクションを上から確認** — 内部管理者のメモ＋「メモを編集する」ボタン（メモ欄の直下）／オプション加入状況／発注者情報＋プラン／職場紹介動画＋投稿ボタン（紫塗り）／基本情報／メッセージ／評判（👍は紫アイコン）／担当者一覧／募集現場一覧＋集計／代理メッセージを見る／アカウント削除／もどる
- [x] **B-7 担当者一覧（法人のみ）** — 鈴木工務店の詳細で、担当者が複数行で並ぶ（氏名・メール・区分・招待中バッジ）。6人目から「もっと見る」
- [x] **B-8 職場紹介動画の投稿（ADM-010B）** — 鈴木工務店の詳細 → 「職場紹介動画」セクションの「職場紹介動画を投稿/編集する」→ ADM-010B（`/admin/users/.../workplace-video`）
  - 確認: 動画URL入力欄がある → URLを入力して更新 → 反映される。「もどる」で ADM-004 に戻る
  - **⚠️ 動画URLは TikTok のみ対応（YouTube/Vimeo は未対応＝仕様）**。テスト用 TikTok URL: `https://www.tiktok.com/@scout2015/video/6718335390845095173`。入力欄の placeholder も TikTok 形式を表示している
  - 補足: このボタンは職場紹介動画オプションに加入している発注者にだけ出ます（鈴木工務店は加入済み）
- [x] **B-9 メモ編集（ADM-005）** — メモ欄の直下「メモを編集する」→ ADM-005 → メモ欄に文章を入力 → 保存 → 詳細に戻ってメモが反映される
  - 確認: メモ欄は十分な高さ（数行分）がある。プレースホルダーは「管理者向けのメモを入力してください」。保存する／もどるボタンは同じ高さ
- [x] **B-10 退会済み発注者の表示** — 一覧で「退会済み組織」を探してクリック
  - OKの目安: 「退会済み」表示（左にアイコン＋縦線マーカー付き）が出て、編集・削除・動画投稿ボタンが**出ない**
- [x] **B-11 スタッフ行クリックは Owner 詳細に解決される** — 一覧で鈴木工務店の担当者（佐藤健太）の行をクリック
  - OKの目安: 担当者単体ではなく、契約主体（鈴木花子＝鈴木工務店）の発注者アカウント詳細（ADM-004）が開く

**ブロック B メモ欄:**
- B-1〜B-5 OK（2026-06-16）。
- 修正対応済み: 一覧系4画面（発注者/ユーザー/応募履歴＋共通 KeywordSearchForm=お問い合わせ/トラブル/求人問い合わせ）の「検索」ボタンを横長＋明るい紫に（`px-10 text-body-md`・`bg-secondary`→`bg-primary`。縦幅は h-11→h-10→**h-9** に調整＝標準高さ。横幅と色で存在感を出す方針）
- ADM-004 修正対応済み: ①「編集する」をページ上部→**メモ欄の直下**へ移動＋ラベルを「**メモを編集する**」に（全体編集に見える誤解を解消。ADM-005 はメモのみ編集の仕様なので整合）②メモ編集・職場紹介動画ボタンを**明るい紫塗り(bg-primary text-white)**に統一③評判の👍3D絵文字→**lucide ThumbsUp 紫アイコン(text-primary/70)**④基本情報 DetailRow のラベルを**薄紫(bg-primary/[0.08])**に（受注者詳細 CLI-006 の InfoRow と統一）⑤「代理メッセージを見る」を**白背景・紫文字・紫枠(outline border-primary text-primary)**に。
- 決定・対応済み⑥ ADM-004 評判の表示を**「また仕事を受けたい good数／合計件数」**に変更（`reputation.goodCount／reputation.total件`。発注者自身のマイページ client-profile と表記統一）。
- 決定・対応済み⑦ ADM-004 募集現場一覧を**「もっと見る／折りたたむ」折りたたみ**に（新部品 `job-site-list.tsx`＝CollapsibleList と同じ UX・**初期5件表示**＝同ページの募集職種 CollapsibleList と閾値を揃えた。鈴木工務店=10件で「もっと見る（残り5件）」が出る）。
- 決定・対応済み⑧ 担当者一覧にも**「もっと見る」を実装**（新部品 `member-list.tsx`・初期5名表示＝**6人目から折りたたみ**・残りは「名」表記）。案件・担当者で UX 統一。閾値5は同ページ募集職種 CollapsibleList と統一（プロジェクト標準＝対応職種/保有資格も5・保有スキルのみ8）。鈴木工務店は担当者5名ちょうどのためボタンは出ない（正常）。**2026-06-16 セッションでデモ用に6人目「デモ 六郎」(demo-member6@test.local)を一時追加し「もっと見る（残り1名）」の動作を確認済み**。この6人目は seed 未登録＝次回 db reset で消える（その後は5名・ボタンなしに戻るのが正常）。B-7 の期待値は本来5名。
- B-8 学び（バグではない）: 動画URLは **TikTok のみ対応**（`src/lib/video-embed.ts` の PATTERNS が tiktok のみ。YouTube/Vimeo は型コメント上「将来」で未実装）。YouTube URL は「対応プラットフォームではない」で正しく弾かれる。フォーム placeholder は TikTok 形式を表示済み。手動テストの例 URL を TikTok に修正。**未決: YouTube/Vimeo 対応を将来追加するかは製品判断（ユーザー保留）**。
- B-9 修正対応済み: ①メモ編集の placeholder「社内向け…」→「**管理者向けのメモを入力してください**」②「保存する」ボタンの `h-12` を外し「もどる」と同じ高さ(h-9)に統一。
- B-10 修正対応済み: 退会済みバナーは**テキスト中央寄せ(text-center)**に変更（当初アイコン＋縦線マーカー案はユーザー却下→撤去）。左の隙間・右寄り感を解消。編集/削除/動画ボタン非表示は従来どおり。
- 別件 修正対応済み: ADM-004 の「発注者情報」セクションの上マージンを `mt-8`→`mt-12`→**`mt-16`** に拡大（ユーザー希望でさらに広げた）。運営の管理情報（メモ／オプション）と受注者に見える発注者情報の間に区切りの余白を追加。（`bg-secondary` ダークパープル→`bg-primary` ブライトパープル＝アプリ共通CTA色に統一）。

---

## ブロック C：管理責任者 招待フロー（ADM-006 / 007）

> 💡 招待メールはローカルのメールボックス（Mailpit: http://127.0.0.1:54324）に届きます。

- [x] **C-1 新規作成画面に移動** — 発注者一覧 →「管理責任者 新規登録」→ `/admin/clients/new`
- [x] **C-2 入力** — 発注者名・姓・名・メール（例: `try-invite@test.local`）を入力 →「入力内容を確認する」
- [x] **C-3 確認画面** — 入力した内容が確認表示される →「作成する」
  - OKの目安: 発注者一覧に戻る
- [x] **C-4 招待メール確認** — Mailpit（http://127.0.0.1:54324）を開く → 招待メールが届いている
- [x] **C-5 パスワード設定** — メール内のリンクを開く → パスワード設定画面 → パスワードを設定
  - ⚠️ リンクは admin からログアウト（またはシークレットウィンドウ）で開く。開いたら別画面に移らず一気にパスワード設定まで進む
  - OKの目安: プラン案内画面（**`/billing` = CLI-026・「申し込む」ボタンあり**）に着地する。ここから申し込み（決済）に進める
  - 📝 2026-06-16 バグ修正: 以前は `/billing/plans`（申し込みボタンの無い比較表＝行き止まり）に着地していた。`acceptInviteAction` の遷移先を `/billing` に修正済み
- [x] **C-6 重複メールはエラーになる** — 再度「管理責任者 新規登録」→ 既に登録済みのメール（例: `contractor@test.local`）で作成を試す（ユーザー確認済み・OK）
  - OKの目安: 「このメールアドレスは既に登録されています」と出て、作成できない

**ブロック C メモ欄:**
- 招待〜パスワード設定〜ログインまでOK（2026-06-16）。
- **バグ発見・修正済み**: 招待された人がパスワード設定後に着地する先が `/billing/plans`（申し込みボタンの無いプラン比較表＝行き止まり）になっており、発注者になるための「申し込む」導線にたどり着けなかった。`acceptInviteAction`（`src/app/(auth)/accept-invite/confirm/actions.ts`）の遷移先を `/billing/plans`→**`/billing`（CLI-026・申し込むボタンあり）**に修正。コメントの意図（CLI-026へ直行）とコードが食い違っていたのが原因。Vitest（accept-invite-action）期待値と e2e/admin.spec.ts の着地URL期待値も更新。tsc・該当Vitest 7/7 パス。
- 補足: try-invite@test.local は一度パスワード設定して「メール確認済み・PW未設定」で残ると同一メール再作成が「登録済み」で弾かれる。やり直す場合は当該 auth.users を削除するか別メール。今回1度 DB から削除して再作成した。
- **決済→発注者化の検証で判明した環境問題（admin バグではない・2026-06-16）**: 招待→/billing→法人プラン申込→Stripe決済は成功するが、その後 role=contractor のまま・subscription なし・display_name 空・org 0 のままだった。原因＝**決済結果を反映する Stripe Webhook がローカルに届いていない**。さらに `stripe listen` 起動を試みると **Stripe CLI のログインキーが期限切れ**（`api_key_expired`）で認証失敗。**キーは2系統**: アプリ用 `.env.local STRIPE_SECRET_KEY`=有効（決済は通る）／CLI ログインキー（`stripe login`/config.toml）=期限切れ。対処はユーザー操作: `stripe login` 後に `stripe listen --forward-to 127.0.0.1:3000/api/webhooks/stripe`（または `stripe listen --api-key <有効sk_test> ...`）。起動時の `whsec_…` が `.env.local STRIPE_WEBHOOK_SECRET`(whsec_3850d6…) と不一致なら .env.local を合わせて dev 再起動。**会社名(invited_company_name=テスト商事)は保存済み＝Webhook が動けば display_name に反映されるはず（プレフィル自体の検証は webhook 稼働後に持ち越し）**。admin の招待フロー（作成→メール→PW→/billing 着地）自体は検証OK。Webhook 稼働後に「決済→発注者化＋会社名プレフィル」を再検証する。
- **✅ Webhook 稼働後の再検証 完了（2026-06-16）**: `stripe listen --api-key <有効sk_test> --forward-to 127.0.0.1:3000/api/webhooks/stripe` で転送起動（whsec_3850d6 一致）。新メール `try-invite2@test.local`（法人・高サポート）で招待→決済を通し、Webhook が 200 で処理 → **role=client / display_name=「テスト商事２」（招待会社名が反映）/ subscription=corporate_premium active / 組織1件作成** を確認。前回の2症状は完全に webhook 未着が原因＝コードは正しい。`stripe listen` はこのセッションのバックグラウンドで稼働中（task）。将来は `stripe login` で CLI キーを正規更新するのが望ましい（今は --api-key で回避中）。

---

## ブロック D：ユーザー（受注者）アカウント管理（ADM-008 / 009 / 010）

- [ ] **D-1 ユーザー一覧が見える** — トップ →「ユーザーアカウント一覧」（`/admin/users`）
  - 確認: 受注者・発注者が並ぶ（担当者・管理者は出ない）。🔍アイコンつき検索欄
- [ ] **D-2 オプションフィルタ** — オプションが3択（動画掲載(受注者PR)／補償¥5,000／補償¥9,800）になっている（職場紹介動画は**ない**＝発注者一覧 ADM-003 側）
- [ ] **D-3 詳細に移動** — 田中一郎（`contractor@test.local`）をクリック → ユーザーアカウント詳細
- [ ] **D-4 本人確認バッジ** — 「本人確認済み」「CCUS登録済み」にタグアイコンが付いている
- [ ] **D-5 評価表示** — 発注者からの評価が ★×5 の7項目＋件数で表示される。補足コメントも見える
- [ ] **D-6 経験年数** — 「{職種} {N}年」の形式で表示される
- [ ] **D-7 PR動画ボタン** — 「受注者PR動画を投稿する」ボタンがある（職場紹介動画ボタンは**ない**＝ADM-004 に移設済み）
- [ ] **D-8 動画投稿（ADM-010）** — D-7 のボタン → 動画URL入力 → 更新 → 登録URLが反映される
- [ ] **D-9 削除ボタンの出し分け** — 受注者（田中一郎）には「アカウントを削除する」が出る。発注者（中村由美など client の人）の詳細には削除ボタンが出ず、「発注者詳細（ADM-004）」への案内リンクが出る（削除は ADM-004 に一本化）
- [ ] **D-10 受注者アカウントの削除（実行・戻せない操作）** — 「削除受注」（`adm-del-contractor@test.local`）の詳細 → 「アカウントを削除する」→ 確認ダイアログ → 削除
  - OKの目安: ユーザー一覧に戻り、「削除受注」が消える（※確認したら次セッション頭のリセットで復活）
  - 補足: この受注者は応募が無いため削除ガードを通過します。進行中取引のある受注者（田中一郎など）で試すと「進行中の案件があるため…」エラーで弾かれます（こちらも確認したいなら別途試してOK）

**ブロック D メモ欄:**
（気づいた点をここに）

---

## ブロック E：本人確認・CCUS 審査（ADM-011 / 012）

- [ ] **E-1 申請一覧** — トップ →「本人確認承認申請一覧」（`/admin/verifications`）
  - 確認: 古い順（山本健 → 井上翔 → 森大地）。各行に種別ラベル（本人確認／CCUS）と年齢
- [ ] **E-2 詳細を開く** — 山本健の行 → 本人確認承認可否（ADM-012）
  - 確認: 本人確認セクション（書類エリア＋否認理由＋否認/承認ボタン）と CCUS登録セクション（「未申請」グレーアウト）
  - 補足: 書類画像はテストデータのため「書類を表示できません」と出ますが正常です
- [ ] **E-3 ボタンの活性条件** — 承認ボタンは最初から押せる。否認ボタンは否認理由を入力すると押せるようになる
- [ ] **E-4 承認する** — 山本健を承認 → 一覧に戻り、山本健が**消える**
- [ ] **E-5 否認する** — 森大地の詳細 → 否認理由を入力 → 否認 → 一覧から消える
- [ ] **E-6 CCUS審査の状態** — 井上翔の詳細 → CCUS登録セクションが審査対象（本人確認済みなので承認可能）になっている
- [ ] **E-7 CCUS を承認する（実行）** — 井上翔の詳細 → CCUS登録セクションの「承認」を押す
  - OKの目安: 一覧に戻り、井上翔が**消える**（CCUS登録済みになった）。※確認したら次セッション頭のリセットで復活

**ブロック E メモ欄:**
（気づいた点をここに）

---

## ブロック F：応募履歴・募集現場（ADM-013 / 014 / 022）

- [ ] **F-1 応募履歴一覧** — トップ →「応募履歴一覧」（`/admin/applications`）
  - 確認: 応募者氏名（年齢）・メール・案件タイトル・初回稼働日・8分類バッジ。🔍検索欄
- [ ] **F-1B キーワード検索（4軸 OR）** — 検索欄（placeholder「氏名・メール・案件タイトル・発注者名」）で確認
  - 「田中」→ 応募者名でヒット、「内装」→ 案件タイトルでヒット、のように氏名でも案件名でも絞り込める
- [ ] **F-2 8分類フィルタ** — ステータスで各分類（応募中／発注済み・初回稼働日前／評価未入力／取引完了／取引不成立／ユーザー側からのキャンセル／運営によるキャンセル／発注側からのお断り）を選んで検索 → 行バッジがその分類と一致する
  - 補足: seed には**全8分類に最低1件**のデータがあります。どれかの分類で0件になったら不具合の可能性大（要メモ）
- [ ] **F-3 並び替え** — 応募日／初回稼働日で昇順・降順を切り替えられる
- [ ] **F-4 詳細を開く（双方向の評価が入った応募）** — 応募ID `aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa`（`/admin/applications/aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa`／田中一郎 × 鈴木工務店）を開く
  - 確認: ステータスバッジ（紫のピル）、案件情報、ユーザー情報、初回勤務日、**ユーザー評価と発注者評価の両方**が表示される（この応募は双方が評価済みなので両方見えるはず）
  - 片側未評価の表示も確認: まだ評価が入っていない応募（例: 応募中・発注済みの行）を開くと、評価欄が「未評価」と表示される
- [ ] **F-5 案件情報の見た目** — 募集職種チップと「N人」がきれいに並んでいる（崩れていない）
- [ ] **F-6 発注取消** — 応募ID `ada00000-0000-4000-8000-000000000004`（`/admin/applications/ada00000-0000-4000-8000-000000000004`）を開く → 「発注を取り消す」→ 確認ダイアログ → 取り消す
  - OKの目安: バッジが「運営によるキャンセル」に変わり、取消ボタンが消える
  - 補足: これは破壊操作です。F-7 / F-8 の閲覧を先に済ませてから実行してOK（リセットで復活）
- [ ] **F-7 募集現場詳細（ADM-022）** — ADM-014 の案件情報、または ADM-004 の募集現場一覧から現場をクリック
  - 確認: 案件内容（タイトル・ステータス・職種・報酬・期間・エリア・詳細）と発注者名。「応募一覧」「発注者詳細」への導線
  - 補足: 案件画像はテストデータに実ファイルが無いためプレースホルダー表示になります（正常）
- [ ] **F-8 ドリルダウン** — ADM-004 の募集現場一覧 → 現場 → ADM-022 →「応募一覧」→ 絞り込まれた応募履歴 → 行クリック → ADM-014
  - 別ルート: ADM-004 の募集現場一覧にある「応募 N件」リンクから、その案件で絞り込んだ応募履歴（ADM-013）に直接飛べることも確認

**ブロック F メモ欄:**
（気づいた点をここに）

---

## ブロック G：問い合わせ3ペア・代理メッセージ・削除（ADM-016〜024 + ADM-004削除）

> ⚠️ **添付ファイル・画像について（G-2 / G-3 / G-7 共通）**
> テストデータには添付の**実ファイルが storage に入っていません**（パスだけ登録）。そのため
> 画像は表示されず（壊れ表示／プレースホルダー）、PDF リンクも開けません。**画像が出ないのは正常**です。
> ここで見るのは「添付枠が出ているか」「画像枠とPDFリンクの**出し分け**が正しいか」だけでOKです（本人確認書類の E-2 と同じ事情）。

### 問い合わせ閲覧
- [ ] **G-1 お問い合わせ一覧（ADM-016）** — `/admin/contacts`
  - 確認: 受信日時・会社名/屋号・氏名・お問い合わせ内容。登録ユーザーには「登録ユーザー」バッジ
- [ ] **G-2 お問い合わせ詳細（ADM-017）** — 添付ありの行（青空電工）を開く
  - 確認: 全項目表示・未入力は「—」。添付は**画像枠（`<img>`）とPDFリンクの出し分け**になっている（※実ファイルが無いので画像は表示されない＝正常。上の注記参照）
  - 登録ユーザーからの問い合わせ（田中一郎／「登録ユーザー」バッジ付きの行）も開く → 「送信ユーザーの詳細を見る」→ ユーザー詳細（ADM-009）に飛ぶ（青空電工は非ログインなのでこのリンクは出ない）
- [ ] **G-3 トラブル報告（ADM-018/019）** — `/admin/trouble-reports` → 詳細（高橋美咲の報告に添付あり）
  - 確認: 添付枠が表示される（※画像は表示されない＝正常。上の注記参照）
- [ ] **G-4 求人問い合わせ（ADM-020/021）** — `/admin/job-inquiries` → 詳細（送信者・宛先発注者名・項目・内容）
  - 導線確認: 「送信者の詳細を見る」→ ADM-009、「宛先発注者の詳細を見る」→ ADM-004 にそれぞれ飛ぶ

### 代理メッセージ
- [ ] **G-5 代理メッセージ一覧（ADM-023）** — `/admin/messages`
  - 確認: 代理メッセージを含むスレッドのみ（鈴木工務店 × 山本健）。会社絞込プルダウン
- [ ] **G-6 ADM-004 からの導線** — 鈴木工務店の詳細 →「代理メッセージを見る」→ その会社で絞り込まれた状態で開く
- [ ] **G-7 メッセージ詳細（ADM-024）** — スレッドを開く
  - 確認: 「代理」バッジが付いたメッセージがある。送信入力欄は**ない**（閲覧専用）
  - 補足: 画像添付のあるメッセージは実ファイルが無いため表示されません（正常。上の注記参照）
- [ ] **G-7B 代理でないスレッドは開けない（ADM-024 ガード）** — アドレスバーに `/admin/messages/eeeeeeee-eeee-eeee-eeee-eeeeeeeeee01`（代理メッセージを含まない通常スレッド）を直接入力
  - OKの目安: 404（管理画面では代理メッセージを含むスレッドしか開けない＝他人の通常会話は覗けない）

### 発注者アカウント削除（最後に・戻せない操作）
- [ ] **G-8 単純削除** — `adm-del-individual@test.local`（削除テスト個人）の詳細 → アカウント削除 → 確認ダイアログ → 削除
  - OKの目安: 発注者一覧に戻り、削除テスト個人が消える
- [ ] **G-9 カスケード削除** — `adm-del-corp-owner@test.local`（削除テスト法人）の詳細 → 削除
  - OKの目安: 発注者一覧に戻り、法人 Owner（削除テスト法人）が消える
  - 連動凍結の確認方法: 一度ログアウトし、配下の担当者 `adm-del-corp-staff@test.local` / `testpass123` でログインを試す → **ログインできなければ**連動凍結OK（確認後リセット）
- [ ] **G-10 削除ガード** — 鈴木工務店（進行中の取引あり）で削除を試みる
  - OKの目安: 「進行中の案件があるため…」のエラーが出て削除できない（※確認したらリセット）

**ブロック G メモ欄:**
（気づいた点をここに）

---

## 進捗トラッカー

最後に完了したブロック・ステップをここに記録しておくと、次セッションで続きから再開できます。

- 最終更新: 2026-06-16（ブロック A・B・C 完了）
- 完了ブロック: A, B, C
- 次にやるブロック: D（受注者アカウント管理 ADM-008/009/010）
- 持ち越しの修正事項:
  - ①全変更が未コミット（全ブロック完了後にまとめてコミット予定。差分が増えてきたので中間コミットも検討可）
  - ②動画プレビューの実サムネ化は後日の専用タスク（[[project_video_thumbnail_feature]]）
  - ③Block C で acceptInviteAction の遷移先バグを修正（/billing/plans→/billing）＋招待→決済→発注者化→会社名反映を Webhook 稼働下で通し検証OK
  - ④ローカルで決済 Webhook を受けるには `stripe listen --api-key <有効sk_test> --forward-to 127.0.0.1:3000/api/webhooks/stripe` が必要（Stripe CLI ログインキーが期限切れのため --api-key で回避中。将来 `stripe login` で正規更新推奨）
  - ⑤このセッションの DB には試験データ（try-invite/try-invite2/デモ六郎/B-8動画/B-9メモ）が残存。Block D 開始時に db reset で一掃する
