# マスタ素材クリーニングノート

このセッション（2026-05-13）で `raw-data/*.txt` を `raw-data/cleaned/*.txt` に整理した記録。
元ファイルは保全のため `raw-data/` 直下に残してある。

## 方針（ユーザー指定）

> 基本的には生データをそのまま使いたい。表記が被っているとか、どちらか一方で良い、とか以外は残したい。

機械的に削除するのは以下のみ:
- **A. skill-tags 内の完全重複**（同じ文字列が複数回登場）
- **B. 表記揺れ**（同義異表記、明らかな typo）
- **C. 他マスタへの統合**（職種マスタ・資格マスタにそのまま同名で存在し、どちらか一方で十分なもの）

それ以外（道具・機械・材料・部品・抽象スキル等）は全て残す。

---

## 1. trade-types（対応職種）

**元: 114行 → cleaned: 113行**（2026-05-13 全件確定、完全 2 階層化済み）

### 構造方針

`大カテゴリ/中カテゴリ｜末端名` の 2 階層に統一（小カテゴリ層を廃止）。

### 反映した修正
| 行 | 原文 | 変更後 | 理由 |
|---|---|---|---|
| 9 | `躯体/コンクリート打設/基礎工事` | `コンクリート打設・基礎工事` | 二重スラッシュ解消（c）+ 小カテ廃止 |
| 14 | `屋根(瓦)` | `屋根（瓦）` | カッコ全角統一（d） |
| 15 | `躯体｜解体/屋根（防水）` | `躯体｜屋根（防水）` | カテゴリ不整合修正（b）+ 小カテ廃止 |
| 25 | `内装/ＬＧＳ` | `LGS` | 英字を半角に統一 + 小カテ廃止 |
| 52 | `付帯工事｜塀�自家発電設備)` | `塀・ブロック工事` | 文字化け復元、ユーザー判断（a） |
| 53-105 全般 | 半角 `()` | 全角『（）』 | カッコ全角統一（d） |
| 96 / 101 | `設計/申請等｜消火設備（消火栓）` が 2 回 | 1 件に統合 | 完全重複 |
| 躯体｜躯体/* (22 件) | 末端のみ残す | `躯体｜大工` 等 | 中カテと小カテ同名の重複削除 |
| 内装｜内装/* (4 件) | 末端のみ残す | `内装｜LGS` 等 | 同上 |
| 仕上げ｜内装/* (9 件) | 末端のみ残す | `仕上げ｜クロス`、`仕上げ｜畳工` 等 | 「仕上げ｜内装/仕上げ　クロス」のような 3 回冗長を解消 |
| 施工管理｜施工管理（*）(5 件) | カッコ削除 | `施工管理｜電気`、`施工管理｜土木` 等 | 大カテと中カテの重複削除後、カッコの意味がないため除去 |
| 末端名内の `/` (4 件) | 中点 `・` に置換 | `溶接/溶断` → `溶接・溶断` / `太陽光/その他` → `太陽光・その他` / `建物電気使用開始/変更申請等` → `建物電気使用開始・変更申請等` / `画像/動画編集` → `画像・動画編集` | 区切り文字 `/` との混同を回避 |
| `ＧＬ工` (1 件) | `GL工` | `建築/内装｜GL工` | LGS との一貫性確保（英字半角化） |

---

## 2. qualifications（保有資格）

**元: 599行 → cleaned: 599行**（追加2 + 削除2 で行数変わらず、2026-05-13 全件確定）

### 反映した修正
| 行 | 内容 | 理由 |
|---|---|---|
| 73 | `3級技能士 配管(建築)` → `3級技能士 配管（建築）` | カッコ全角統一 |
| 74 前 | `1級技能士 内装仕上げ施工（プラスチック系床）` `2級技能士 内装仕上げ施工（プラスチック系床）` を追加 | 1/2級欠落の補完（b） |
| 旧 358/359 | `1級技能士` `2級技能士` を削除 | 抽象表記、具体表記に統合済み（a） |
| 全般 | 半角 `()` を全角『（）』に統一 | 3 マスタ間でカッコ表記を揃える（例: 行 31, 32 `衛生管理者(第1種)(第2種)` 等） |

※ メモリに記載のあった「`足場の組立等作業主任者` の重複」は skill-tags 側の値と qualifications 側 `足場の組立て等作業主任者（技能講習）`（行 592）の混同。qualifications 内に同名重複はなし

---

## 3. skill-tags（保有スキル）

**元: 277行 → cleaned: 244行**（2026-05-13 判断確定）

### 反映した修正
| 内容 | 件数 |
|---|---|
| A. 完全重複「施工図作成」 2 回 → 1 件 | -1 |
| A. 完全重複「品質管理」 2 回 → 1 件 | -1 |
| C. 職種マスタへ統合（B-1） | -26 |
| C. 資格マスタへ統合（B-2） | -5 |
| 表記統一: 半角 `()` を全角『（）』に統一 | 多数行 |

道具・機械・材料・抽象語は全て残してある（生データそのまま）。

### B-1. 職種マスタへ統合確定（26 件、2026-05-13）

skill-tags から削除し、trade-types 側で表現する:

| 削除項目 | 統合先 trade-types |
|---|---|
| 左官 | 建築/仕上げ｜内装/左官工 |
| カーポート | 建築/付帯工事｜カーポート |
| あと施工アンカー工 | 建築/躯体｜躯体/アンカー工 |
| アンカー工 | 建築/躯体｜躯体/アンカー工 |
| ハウスクリーニング工 | 建築/仕上げ｜クリーニング |
| 足場とび工 | 建築/躯体｜躯体/足場工 |
| 重量とび工 | 建築/躯体｜躯体/重量鳶 |
| 電気工 | 設備/施工｜電気(その他全般) |
| 鉄筋工 | 建築/躯体｜躯体/鉄筋工 |
| 塗装工 | 建築/仕上げ｜内装/塗装工 |
| 溶接工 | 設備/施工｜溶接/溶断 |
| 型わく工 | 建築/躯体｜躯体/型枠工（表記揺れ「わく」⇔「枠」） |
| 宮大工 | 建築/躯体｜躯体/宮大工 |
| 左官工 | 建築/仕上げ｜内装/左官工 |
| ポンプ設置工 | 建築/躯体｜躯体/ポンプ工 |
| サッシ工 | 建築/躯体｜躯体/サッシ（アルミ/スチール 等） |
| かわらぶき工 | 建築/躯体｜躯体/屋根(瓦) |
| 建具工 | 建築/仕上げ｜建具 |
| 昇降機技能工(エレベータ工) | 建築/躯体｜躯体/サッシ（ELV・エスカレーター） |
| 昇降機技能工(エスカレーター工) | 同上 |
| ウッドデッキ工 | 建築/付帯工事｜ウッドデッキ |
| 建築設計・デザイン | 設計/申請等｜建築設計 + 建築デザイン |
| 現場監督（建築） | 施工管理｜施工管理（建築） |
| 現場監督（土木） | 施工管理｜施工管理（土木） |
| 基礎工事 | 建築/躯体｜躯体/コンクリート打設/基礎工事 |
| 大工（リフォーム） | 建築/躯体｜躯体/大工（のサブセット） |

### B-2. 資格マスタへ統合確定（5 件、2026-05-13）

| 削除項目 | 統合先 qualifications |
|---|---|
| 玉掛け | 玉掛技能者（1t以上）/（1t未満）/ 玉掛業務従事者 |
| フォークリフト運転 | フォークリフト運転員（1t以上）/（1t未満） |
| 小型移動式クレーン運転 | 小型移動式クレーン運転（つり上げ荷重5t未満） |
| 高所作業車運転 | 高所作業車運転手（作業床10m未満）/ 高所作業車運転技能講習（作業床10m以上） |
| 足場の組立等作業主任者 | 足場の組立て等作業主任者（技能講習） |

---

## 4. 次のアクション

クリーニング完了。次は `/kiro:spec-init master-skills` 実行 → requirements.md で 3 マスタの使い分けと UI 仕様を確定する段階。

## 5. 影響範囲（マスタ確定後の書き換え対象）

- 定数: `src/lib/constants/options.ts`（TRADE_TYPES 拡張 + SKILL_TAGS / QUALIFICATIONS を追加）
- DB: `user_skills.trade_type` の値域、`users.skill_tags text[]` の自由入力 → マスタ化検討、`user_qualifications.qualification_name` の値域
- 画面: register/profile、COM-002、CLI-005（検索ポップアップ）、CON-002（検索）、CLI-001、job-form
- バリデーション: `src/lib/validations/profile.ts`
- データ: `supabase/seed.sql`
- E2E: skill / qualification 関連テスト
